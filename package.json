{
  "name": "sview",
  "version": "0.1.0",
  "description": "Strava View",
  "main": "index.js",
  "scripts": {
    "start": "babel-node server.js --presets es2015",
    "tsbuild": "tsc -p ./tsconfig.json",
    "build": "npm run tsbuild && webpack",
    "test": "mocha --require babel-core/register",
    "expose-db": "sudo docker compose -f ./docker compose.yml -f ./docker compose.testing.yml up -d",
    "hide-db": "sudo docker compose up -d",
    "testdb": "npm run expose-db && npm run test && npm run hide-db",
    "build-container": "npm run build && sudo docker compose build",
    "deploy": "npm run build-container && sudo docker compose up -d",
    "dump-db": "sudo docker run -it --rm --network=sview_internal postgres pg_dump -h pgdb --inserts -U postgres running > running_db.sql",
    "restore-db": "sudo docker run -i --rm --network=sview_internal postgres psql -h pgdb -d postgres -U postgres -c \"DROP DATABASE IF EXISTS running;\" &&  sudo docker run -i --rm --network=sview_internal postgres psql -h pgdb -d postgres -U postgres -c \"CREATE DATABASE running;\" && sudo docker run -i --rm --network=sview_internal postgres psql -h pgdb -d running -U postgres < running_db.sql",
    "dump-temp-db": "npm run expose-db && babel-node --presets es2015 scripts/setup_db.js && pg_dump --inserts -U postgres temp_db > temp_db.sql && babel-node --presets es2015 scripts/cleanup_db.js && npm run hide-db",
    "sql": "sudo docker run -it --rm --network=sview_internal postgres psql -h pgdb -U postgres",
    "connect": "sudo docker exec -it sview-sview-1 /bin/bash",
    "connect-db": "sudo docker exec -it sview-pgdb-1 psql -U postgres -d running"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chrisjhh/sview.git"
  },
  "keywords": [
    "strava",
    "view"
  ],
  "author": "Chris Hamer-Hodges",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/chrisjhh/sview/issues"
  },
  "homepage": "https://github.com/chrisjhh/sview#readme",
  "dependencies": {
    "@types/pg": "^8.15.5",
    "babel-loader": "^10.0.0",
    "chroma-js": "^3.1.2",
    "express": "^5.1.0",
    "geolib": "^3.3.4",
    "http": "^0.0.1-security",
    "https": "^1.0.0",
    "kalmanjs": "^1.1.0",
    "leaflet": "^1.9.4",
    "pg": "^8.16.3",
    "prop-types": "^15.8.1",
    "querystring": "^0.2.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "simplify-js": "^1.2.4",
    "suncalc": "^1.9.0",
    "webpack": "^5.101.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "chai": "^6.0.1",
    "fs": "0.0.1-security",
    "mocha": "^11.7.1",
    "path": "^0.12.7",
    "sinon": "^21.0.0",
    "tslint": "^5.20.1",
    "typescript": "^5.9.2",
    "webpack-cli": "^6.0.1"
  }
}
